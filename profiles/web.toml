# Web work needs the whole web category, but only the discovery half of recon —
# a full port-scanning suite is not the point here.

[profile]
id = "web"
name = "Web Hacking"
description = "Intercepting proxies, injection testing and content discovery"
includes = ["baseline"]
categories = ["sec-web"]
tools = ["ffuf", "gobuster", "httpx", "nmap"]
